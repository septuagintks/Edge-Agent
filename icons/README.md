# Icons

The MV3 manifest does not currently reference icon files (Edge will show a default puzzle-piece icon during development).

When ready, add PNGs here and re-add the `icons` and `action.default_icon` blocks to `manifest.json`:

```
icons/
  icon-16.png
  icon-32.png
  icon-48.png
  icon-128.png
```
